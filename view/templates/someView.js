define( [ 'backbone' ], function( Backbone )
{
    'use strict';
    
    var <%= name %> = Backbone.View.extend(
    {
       template : '',

        /**
         * init
         */
        initialize : function()
        {
        }
        
    });
    return <%= name %>;
});

