define( [ 'backbone' ], function( Backbone )
{
    'use strict';

    /**
     * AppView - main application view
     */
    var AppView = Backbone.View.extend(
    {
        template    : 'appView',
        initialize  : function()
        {
            // init 
        }

    });
    return AppView;
});